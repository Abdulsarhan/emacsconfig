# This is an emacs config that uses the elpaca package manager and evil mode
## Keybindings. 
The keybindings should be fairly easy to understand. Just read the config file. 
some of the keybindings are commented out becuase I don't use them anymore, but they would work just find if you remove the comments. 
